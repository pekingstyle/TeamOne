<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>cn.teamone</groupId>
    <artifactId>teamone-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>
  <artifactId>teamone-prd</artifactId>
  <name>TeamOne :: PRD</name>
  <description>产品研发域：目标/需求/RoadMap/版本/迭代/工作项/冲突（M1 实现，见架构设计 §6.1）</description>
  <dependencies>
    <dependency>
      <groupId>cn.teamone</groupId>
      <artifactId>teamone-shared</artifactId>
    </dependency>
    <!-- W2-B4: 门禁/状态机使用 platform SPI（PermissionService/OutboxWriter/IdempotencyService/AuditService
         + AuditableListener），架构设计 §1.4 允许 prd→platform 单向依赖 -->
    <dependency>
      <groupId>cn.teamone</groupId>
      <artifactId>teamone-platform</artifactId>
    </dependency>
    <!-- W2: JPA 实体/仓库 + JdbcTemplate（key_sequence 行锁发号，05 §2.4） -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <!-- W2-B6: TransitionTableTest 纯逻辑单测 -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>
</project>
