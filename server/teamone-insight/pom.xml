<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 https://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>cn.teamone</groupId>
    <artifactId>teamone-parent</artifactId>
    <version>0.1.0-SNAPSHOT</version>
  </parent>
  <artifactId>teamone-insight</artifactId>
  <name>TeamOne :: Insight</name>
  <description>概览域：冲突快照批算/报表物化/活动流/工作台聚合（M2 实现，纯消费者）</description>
  <dependencies>
    <dependency>
      <groupId>cn.teamone</groupId>
      <artifactId>teamone-shared</artifactId>
    </dependency>
    <!-- M2-INC-1 W2：OutboxWriter/AuditService SPI（架构设计 §1.4 允许 insight→platform 单向，
         ArchUnit R6 允许范围：platform + shared，零 prd/collab/eng/app import） -->
    <dependency>
      <groupId>cn.teamone</groupId>
      <artifactId>teamone-platform</artifactId>
    </dependency>
    <!-- M2-INC-1 W2：只读映射实体/仓库 + conflict_snapshot 快照写（JPA） -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-data-jpa</artifactId>
    </dependency>
    <!-- M2-INC-1 W2：ConflictEngine 原型基线单测（纯逻辑，无 Spring） -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-starter-test</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>
</project>
